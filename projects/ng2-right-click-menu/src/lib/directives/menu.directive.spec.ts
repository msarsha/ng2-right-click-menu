import { MenuDirective } from './ng2rcm-menu.directive';

describe('Ng2rcmMenuDirective', () => {
	it('should create an instance', () => {
		const directive = new MenuDirective();
		expect(directive).toBeTruthy();
	});
});
